source [find board/stm32f4discovery.cfg]

# Non-intrusive SRAM reads for the deploy harness. The target core keeps
# running while OpenOCD reads the exported status word through the AHB-AP.
target create sram_monitor mem_ap -dap $_CHIPNAME.dap -ap-num 0
